const initialData = {
    list :[],
};
const Reducer = (state = initialData, action)=>{
    switch (action.type) {
      case "ActionTypeAddtodo":
        {
            const (action,data)=action.payload;
        }
    }
}